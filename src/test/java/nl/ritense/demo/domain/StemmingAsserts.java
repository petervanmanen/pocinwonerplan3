package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class StemmingAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStemmingAllPropertiesEquals(Stemming expected, Stemming actual) {
        assertStemmingAutoGeneratedPropertiesEquals(expected, actual);
        assertStemmingAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStemmingAllUpdatablePropertiesEquals(Stemming expected, Stemming actual) {
        assertStemmingUpdatableFieldsEquals(expected, actual);
        assertStemmingUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStemmingAutoGeneratedPropertiesEquals(Stemming expected, Stemming actual) {
        assertThat(expected)
            .as("Verify Stemming auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStemmingUpdatableFieldsEquals(Stemming expected, Stemming actual) {
        assertThat(expected)
            .as("Verify Stemming relevant properties")
            .satisfies(e -> assertThat(e.getResultaat()).as("check resultaat").isEqualTo(actual.getResultaat()))
            .satisfies(e -> assertThat(e.getStemmingstype()).as("check stemmingstype").isEqualTo(actual.getStemmingstype()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStemmingUpdatableRelationshipsEquals(Stemming expected, Stemming actual) {
        assertThat(expected)
            .as("Verify Stemming relationships")
            .satisfies(e -> assertThat(e.getBetreftRaadsstuk()).as("check betreftRaadsstuk").isEqualTo(actual.getBetreftRaadsstuk()))
            .satisfies(e -> assertThat(e.getHoortbijAgendapunt()).as("check hoortbijAgendapunt").isEqualTo(actual.getHoortbijAgendapunt()));
    }
}
