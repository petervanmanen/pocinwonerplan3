package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ExamenonderdeelAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertExamenonderdeelAllPropertiesEquals(Examenonderdeel expected, Examenonderdeel actual) {
        assertExamenonderdeelAutoGeneratedPropertiesEquals(expected, actual);
        assertExamenonderdeelAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertExamenonderdeelAllUpdatablePropertiesEquals(Examenonderdeel expected, Examenonderdeel actual) {
        assertExamenonderdeelUpdatableFieldsEquals(expected, actual);
        assertExamenonderdeelUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertExamenonderdeelAutoGeneratedPropertiesEquals(Examenonderdeel expected, Examenonderdeel actual) {
        assertThat(expected)
            .as("Verify Examenonderdeel auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertExamenonderdeelUpdatableFieldsEquals(Examenonderdeel expected, Examenonderdeel actual) {}

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertExamenonderdeelUpdatableRelationshipsEquals(Examenonderdeel expected, Examenonderdeel actual) {
        assertThat(expected)
            .as("Verify Examenonderdeel relationships")
            .satisfies(e -> assertThat(e.getEmptyExamen()).as("check emptyExamen").isEqualTo(actual.getEmptyExamen()));
    }
}
