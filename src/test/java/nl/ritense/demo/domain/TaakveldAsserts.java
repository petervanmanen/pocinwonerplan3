package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class TaakveldAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTaakveldAllPropertiesEquals(Taakveld expected, Taakveld actual) {
        assertTaakveldAutoGeneratedPropertiesEquals(expected, actual);
        assertTaakveldAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTaakveldAllUpdatablePropertiesEquals(Taakveld expected, Taakveld actual) {
        assertTaakveldUpdatableFieldsEquals(expected, actual);
        assertTaakveldUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTaakveldAutoGeneratedPropertiesEquals(Taakveld expected, Taakveld actual) {
        assertThat(expected)
            .as("Verify Taakveld auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTaakveldUpdatableFieldsEquals(Taakveld expected, Taakveld actual) {
        assertThat(expected)
            .as("Verify Taakveld relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertTaakveldUpdatableRelationshipsEquals(Taakveld expected, Taakveld actual) {
        assertThat(expected)
            .as("Verify Taakveld relationships")
            .satisfies(e -> assertThat(e.getHeeftKostenplaats()).as("check heeftKostenplaats").isEqualTo(actual.getHeeftKostenplaats()));
    }
}
