package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ResultaatsoortAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertResultaatsoortAllPropertiesEquals(Resultaatsoort expected, Resultaatsoort actual) {
        assertResultaatsoortAutoGeneratedPropertiesEquals(expected, actual);
        assertResultaatsoortAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertResultaatsoortAllUpdatablePropertiesEquals(Resultaatsoort expected, Resultaatsoort actual) {
        assertResultaatsoortUpdatableFieldsEquals(expected, actual);
        assertResultaatsoortUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertResultaatsoortAutoGeneratedPropertiesEquals(Resultaatsoort expected, Resultaatsoort actual) {
        assertThat(expected)
            .as("Verify Resultaatsoort auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertResultaatsoortUpdatableFieldsEquals(Resultaatsoort expected, Resultaatsoort actual) {
        assertThat(expected)
            .as("Verify Resultaatsoort relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertResultaatsoortUpdatableRelationshipsEquals(Resultaatsoort expected, Resultaatsoort actual) {}
}
