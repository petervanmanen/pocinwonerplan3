package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ClassaAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClassaAllPropertiesEquals(Classa expected, Classa actual) {
        assertClassaAutoGeneratedPropertiesEquals(expected, actual);
        assertClassaAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClassaAllUpdatablePropertiesEquals(Classa expected, Classa actual) {
        assertClassaUpdatableFieldsEquals(expected, actual);
        assertClassaUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClassaAutoGeneratedPropertiesEquals(Classa expected, Classa actual) {
        assertThat(expected)
            .as("Verify Classa auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClassaUpdatableFieldsEquals(Classa expected, Classa actual) {
        assertThat(expected)
            .as("Verify Classa relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertClassaUpdatableRelationshipsEquals(Classa expected, Classa actual) {}
}
