package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class LeerjaarAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeerjaarAllPropertiesEquals(Leerjaar expected, Leerjaar actual) {
        assertLeerjaarAutoGeneratedPropertiesEquals(expected, actual);
        assertLeerjaarAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeerjaarAllUpdatablePropertiesEquals(Leerjaar expected, Leerjaar actual) {
        assertLeerjaarUpdatableFieldsEquals(expected, actual);
        assertLeerjaarUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeerjaarAutoGeneratedPropertiesEquals(Leerjaar expected, Leerjaar actual) {
        assertThat(expected)
            .as("Verify Leerjaar auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeerjaarUpdatableFieldsEquals(Leerjaar expected, Leerjaar actual) {
        assertThat(expected)
            .as("Verify Leerjaar relevant properties")
            .satisfies(e -> assertThat(e.getJaareinde()).as("check jaareinde").isEqualTo(actual.getJaareinde()))
            .satisfies(e -> assertThat(e.getJaarstart()).as("check jaarstart").isEqualTo(actual.getJaarstart()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeerjaarUpdatableRelationshipsEquals(Leerjaar expected, Leerjaar actual) {}
}
