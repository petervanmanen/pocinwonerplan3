package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class StadspasAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStadspasAllPropertiesEquals(Stadspas expected, Stadspas actual) {
        assertStadspasAutoGeneratedPropertiesEquals(expected, actual);
        assertStadspasAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStadspasAllUpdatablePropertiesEquals(Stadspas expected, Stadspas actual) {
        assertStadspasUpdatableFieldsEquals(expected, actual);
        assertStadspasUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStadspasAutoGeneratedPropertiesEquals(Stadspas expected, Stadspas actual) {
        assertThat(expected)
            .as("Verify Stadspas auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStadspasUpdatableFieldsEquals(Stadspas expected, Stadspas actual) {
        assertThat(expected)
            .as("Verify Stadspas relevant properties")
            .satisfies(e -> assertThat(e.getDatumeinde()).as("check datumeinde").isEqualTo(actual.getDatumeinde()))
            .satisfies(e -> assertThat(e.getDatumstart()).as("check datumstart").isEqualTo(actual.getDatumstart()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStadspasUpdatableRelationshipsEquals(Stadspas expected, Stadspas actual) {}
}
