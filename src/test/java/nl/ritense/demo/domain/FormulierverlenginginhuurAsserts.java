package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class FormulierverlenginginhuurAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormulierverlenginginhuurAllPropertiesEquals(
        Formulierverlenginginhuur expected,
        Formulierverlenginginhuur actual
    ) {
        assertFormulierverlenginginhuurAutoGeneratedPropertiesEquals(expected, actual);
        assertFormulierverlenginginhuurAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormulierverlenginginhuurAllUpdatablePropertiesEquals(
        Formulierverlenginginhuur expected,
        Formulierverlenginginhuur actual
    ) {
        assertFormulierverlenginginhuurUpdatableFieldsEquals(expected, actual);
        assertFormulierverlenginginhuurUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormulierverlenginginhuurAutoGeneratedPropertiesEquals(
        Formulierverlenginginhuur expected,
        Formulierverlenginginhuur actual
    ) {
        assertThat(expected)
            .as("Verify Formulierverlenginginhuur auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormulierverlenginginhuurUpdatableFieldsEquals(
        Formulierverlenginginhuur expected,
        Formulierverlenginginhuur actual
    ) {
        assertThat(expected)
            .as("Verify Formulierverlenginginhuur relevant properties")
            .satisfies(e -> assertThat(e.getDatumeindenieuw()).as("check datumeindenieuw").isEqualTo(actual.getDatumeindenieuw()))
            .satisfies(
                e ->
                    assertThat(e.getIndicatieredeninhuurgewijzigd())
                        .as("check indicatieredeninhuurgewijzigd")
                        .isEqualTo(actual.getIndicatieredeninhuurgewijzigd())
            )
            .satisfies(
                e ->
                    assertThat(e.getIndicatieverhogeninkooporder())
                        .as("check indicatieverhogeninkooporder")
                        .isEqualTo(actual.getIndicatieverhogeninkooporder())
            )
            .satisfies(e -> assertThat(e.getToelichting()).as("check toelichting").isEqualTo(actual.getToelichting()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormulierverlenginginhuurUpdatableRelationshipsEquals(
        Formulierverlenginginhuur expected,
        Formulierverlenginginhuur actual
    ) {}
}
