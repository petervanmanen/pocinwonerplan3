package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class FunctieAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFunctieAllPropertiesEquals(Functie expected, Functie actual) {
        assertFunctieAutoGeneratedPropertiesEquals(expected, actual);
        assertFunctieAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFunctieAllUpdatablePropertiesEquals(Functie expected, Functie actual) {
        assertFunctieUpdatableFieldsEquals(expected, actual);
        assertFunctieUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFunctieAutoGeneratedPropertiesEquals(Functie expected, Functie actual) {
        assertThat(expected)
            .as("Verify Functie auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFunctieUpdatableFieldsEquals(Functie expected, Functie actual) {
        assertThat(expected)
            .as("Verify Functie relevant properties")
            .satisfies(e -> assertThat(e.getGroep()).as("check groep").isEqualTo(actual.getGroep()))
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFunctieUpdatableRelationshipsEquals(Functie expected, Functie actual) {}
}
