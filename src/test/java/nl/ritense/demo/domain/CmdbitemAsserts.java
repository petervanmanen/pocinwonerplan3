package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CmdbitemAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCmdbitemAllPropertiesEquals(Cmdbitem expected, Cmdbitem actual) {
        assertCmdbitemAutoGeneratedPropertiesEquals(expected, actual);
        assertCmdbitemAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCmdbitemAllUpdatablePropertiesEquals(Cmdbitem expected, Cmdbitem actual) {
        assertCmdbitemUpdatableFieldsEquals(expected, actual);
        assertCmdbitemUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCmdbitemAutoGeneratedPropertiesEquals(Cmdbitem expected, Cmdbitem actual) {
        assertThat(expected)
            .as("Verify Cmdbitem auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCmdbitemUpdatableFieldsEquals(Cmdbitem expected, Cmdbitem actual) {
        assertThat(expected)
            .as("Verify Cmdbitem relevant properties")
            .satisfies(e -> assertThat(e.getBeschrijving()).as("check beschrijving").isEqualTo(actual.getBeschrijving()))
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCmdbitemUpdatableRelationshipsEquals(Cmdbitem expected, Cmdbitem actual) {}
}
