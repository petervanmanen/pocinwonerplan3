package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class BelijningAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelijningAllPropertiesEquals(Belijning expected, Belijning actual) {
        assertBelijningAutoGeneratedPropertiesEquals(expected, actual);
        assertBelijningAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelijningAllUpdatablePropertiesEquals(Belijning expected, Belijning actual) {
        assertBelijningUpdatableFieldsEquals(expected, actual);
        assertBelijningUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelijningAutoGeneratedPropertiesEquals(Belijning expected, Belijning actual) {
        assertThat(expected)
            .as("Verify Belijning auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelijningUpdatableFieldsEquals(Belijning expected, Belijning actual) {
        assertThat(expected)
            .as("Verify Belijning relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelijningUpdatableRelationshipsEquals(Belijning expected, Belijning actual) {
        assertThat(expected)
            .as("Verify Belijning relationships")
            .satisfies(
                e -> assertThat(e.getHeeftBinnenlocaties()).as("check heeftBinnenlocaties").isEqualTo(actual.getHeeftBinnenlocaties())
            )
            .satisfies(e -> assertThat(e.getHeeftVelds()).as("check heeftVelds").isEqualTo(actual.getHeeftVelds()));
    }
}
