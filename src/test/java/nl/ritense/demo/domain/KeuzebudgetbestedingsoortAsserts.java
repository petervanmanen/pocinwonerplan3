package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class KeuzebudgetbestedingsoortAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeuzebudgetbestedingsoortAllPropertiesEquals(
        Keuzebudgetbestedingsoort expected,
        Keuzebudgetbestedingsoort actual
    ) {
        assertKeuzebudgetbestedingsoortAutoGeneratedPropertiesEquals(expected, actual);
        assertKeuzebudgetbestedingsoortAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeuzebudgetbestedingsoortAllUpdatablePropertiesEquals(
        Keuzebudgetbestedingsoort expected,
        Keuzebudgetbestedingsoort actual
    ) {
        assertKeuzebudgetbestedingsoortUpdatableFieldsEquals(expected, actual);
        assertKeuzebudgetbestedingsoortUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeuzebudgetbestedingsoortAutoGeneratedPropertiesEquals(
        Keuzebudgetbestedingsoort expected,
        Keuzebudgetbestedingsoort actual
    ) {
        assertThat(expected)
            .as("Verify Keuzebudgetbestedingsoort auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeuzebudgetbestedingsoortUpdatableFieldsEquals(
        Keuzebudgetbestedingsoort expected,
        Keuzebudgetbestedingsoort actual
    ) {
        assertThat(expected)
            .as("Verify Keuzebudgetbestedingsoort relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeuzebudgetbestedingsoortUpdatableRelationshipsEquals(
        Keuzebudgetbestedingsoort expected,
        Keuzebudgetbestedingsoort actual
    ) {}
}
