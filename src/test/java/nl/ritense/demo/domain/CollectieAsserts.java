package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CollectieAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCollectieAllPropertiesEquals(Collectie expected, Collectie actual) {
        assertCollectieAutoGeneratedPropertiesEquals(expected, actual);
        assertCollectieAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCollectieAllUpdatablePropertiesEquals(Collectie expected, Collectie actual) {
        assertCollectieUpdatableFieldsEquals(expected, actual);
        assertCollectieUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCollectieAutoGeneratedPropertiesEquals(Collectie expected, Collectie actual) {
        assertThat(expected)
            .as("Verify Collectie auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCollectieUpdatableFieldsEquals(Collectie expected, Collectie actual) {
        assertThat(expected)
            .as("Verify Collectie relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCollectieUpdatableRelationshipsEquals(Collectie expected, Collectie actual) {
        assertThat(expected)
            .as("Verify Collectie relationships")
            .satisfies(e -> assertThat(e.getBevatMuseumobjects()).as("check bevatMuseumobjects").isEqualTo(actual.getBevatMuseumobjects()));
    }
}
