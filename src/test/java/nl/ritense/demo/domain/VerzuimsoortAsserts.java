package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class VerzuimsoortAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertVerzuimsoortAllPropertiesEquals(Verzuimsoort expected, Verzuimsoort actual) {
        assertVerzuimsoortAutoGeneratedPropertiesEquals(expected, actual);
        assertVerzuimsoortAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertVerzuimsoortAllUpdatablePropertiesEquals(Verzuimsoort expected, Verzuimsoort actual) {
        assertVerzuimsoortUpdatableFieldsEquals(expected, actual);
        assertVerzuimsoortUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertVerzuimsoortAutoGeneratedPropertiesEquals(Verzuimsoort expected, Verzuimsoort actual) {
        assertThat(expected)
            .as("Verify Verzuimsoort auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertVerzuimsoortUpdatableFieldsEquals(Verzuimsoort expected, Verzuimsoort actual) {
        assertThat(expected)
            .as("Verify Verzuimsoort relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertVerzuimsoortUpdatableRelationshipsEquals(Verzuimsoort expected, Verzuimsoort actual) {}
}
