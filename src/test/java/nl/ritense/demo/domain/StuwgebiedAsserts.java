package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class StuwgebiedAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStuwgebiedAllPropertiesEquals(Stuwgebied expected, Stuwgebied actual) {
        assertStuwgebiedAutoGeneratedPropertiesEquals(expected, actual);
        assertStuwgebiedAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStuwgebiedAllUpdatablePropertiesEquals(Stuwgebied expected, Stuwgebied actual) {
        assertStuwgebiedUpdatableFieldsEquals(expected, actual);
        assertStuwgebiedUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStuwgebiedAutoGeneratedPropertiesEquals(Stuwgebied expected, Stuwgebied actual) {
        assertThat(expected)
            .as("Verify Stuwgebied auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStuwgebiedUpdatableFieldsEquals(Stuwgebied expected, Stuwgebied actual) {
        assertThat(expected)
            .as("Verify Stuwgebied relevant properties")
            .satisfies(e -> assertThat(e.getBemalingsgebied()).as("check bemalingsgebied").isEqualTo(actual.getBemalingsgebied()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertStuwgebiedUpdatableRelationshipsEquals(Stuwgebied expected, Stuwgebied actual) {}
}
