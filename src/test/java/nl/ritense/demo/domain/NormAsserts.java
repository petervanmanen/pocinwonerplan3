package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class NormAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNormAllPropertiesEquals(Norm expected, Norm actual) {
        assertNormAutoGeneratedPropertiesEquals(expected, actual);
        assertNormAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNormAllUpdatablePropertiesEquals(Norm expected, Norm actual) {
        assertNormUpdatableFieldsEquals(expected, actual);
        assertNormUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNormAutoGeneratedPropertiesEquals(Norm expected, Norm actual) {
        assertThat(expected)
            .as("Verify Norm auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNormUpdatableFieldsEquals(Norm expected, Norm actual) {
        assertThat(expected)
            .as("Verify Norm relevant properties")
            .satisfies(e -> assertThat(e.getNen3610id()).as("check nen3610id").isEqualTo(actual.getNen3610id()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNormUpdatableRelationshipsEquals(Norm expected, Norm actual) {}
}
