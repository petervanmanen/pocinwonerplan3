package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class OmgevingsnormAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmgevingsnormAllPropertiesEquals(Omgevingsnorm expected, Omgevingsnorm actual) {
        assertOmgevingsnormAutoGeneratedPropertiesEquals(expected, actual);
        assertOmgevingsnormAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmgevingsnormAllUpdatablePropertiesEquals(Omgevingsnorm expected, Omgevingsnorm actual) {
        assertOmgevingsnormUpdatableFieldsEquals(expected, actual);
        assertOmgevingsnormUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmgevingsnormAutoGeneratedPropertiesEquals(Omgevingsnorm expected, Omgevingsnorm actual) {
        assertThat(expected)
            .as("Verify Omgevingsnorm auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmgevingsnormUpdatableFieldsEquals(Omgevingsnorm expected, Omgevingsnorm actual) {
        assertThat(expected)
            .as("Verify Omgevingsnorm relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmgevingsnormgroep()).as("check omgevingsnormgroep").isEqualTo(actual.getOmgevingsnormgroep()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmgevingsnormUpdatableRelationshipsEquals(Omgevingsnorm expected, Omgevingsnorm actual) {
        assertThat(expected)
            .as("Verify Omgevingsnorm relationships")
            .satisfies(
                e ->
                    assertThat(e.getBeschrijftOmgevingswaarderegels())
                        .as("check beschrijftOmgevingswaarderegels")
                        .isEqualTo(actual.getBeschrijftOmgevingswaarderegels())
            );
    }
}
