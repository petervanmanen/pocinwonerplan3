package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ErouteAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertErouteAllPropertiesEquals(Eroute expected, Eroute actual) {
        assertErouteAutoGeneratedPropertiesEquals(expected, actual);
        assertErouteAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertErouteAllUpdatablePropertiesEquals(Eroute expected, Eroute actual) {
        assertErouteUpdatableFieldsEquals(expected, actual);
        assertErouteUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertErouteAutoGeneratedPropertiesEquals(Eroute expected, Eroute actual) {
        assertThat(expected)
            .as("Verify Eroute auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertErouteUpdatableFieldsEquals(Eroute expected, Eroute actual) {
        assertThat(expected)
            .as("Verify Eroute relevant properties")
            .satisfies(e -> assertThat(e.getGeometrie()).as("check geometrie").isEqualTo(actual.getGeometrie()))
            .satisfies(e -> assertThat(e.getEroutecode()).as("check eroutecode").isEqualTo(actual.getEroutecode()))
            .satisfies(e -> assertThat(e.getEroutesoort()).as("check eroutesoort").isEqualTo(actual.getEroutesoort()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertErouteUpdatableRelationshipsEquals(Eroute expected, Eroute actual) {}
}
