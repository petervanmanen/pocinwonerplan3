package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ProgrammasoortAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertProgrammasoortAllPropertiesEquals(Programmasoort expected, Programmasoort actual) {
        assertProgrammasoortAutoGeneratedPropertiesEquals(expected, actual);
        assertProgrammasoortAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertProgrammasoortAllUpdatablePropertiesEquals(Programmasoort expected, Programmasoort actual) {
        assertProgrammasoortUpdatableFieldsEquals(expected, actual);
        assertProgrammasoortUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertProgrammasoortAutoGeneratedPropertiesEquals(Programmasoort expected, Programmasoort actual) {
        assertThat(expected)
            .as("Verify Programmasoort auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertProgrammasoortUpdatableFieldsEquals(Programmasoort expected, Programmasoort actual) {
        assertThat(expected)
            .as("Verify Programmasoort relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertProgrammasoortUpdatableRelationshipsEquals(Programmasoort expected, Programmasoort actual) {
        assertThat(expected)
            .as("Verify Programmasoort relationships")
            .satisfies(e -> assertThat(e.getVoorProgrammas()).as("check voorProgrammas").isEqualTo(actual.getVoorProgrammas()));
    }
}
