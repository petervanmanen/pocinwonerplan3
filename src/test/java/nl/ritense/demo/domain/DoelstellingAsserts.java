package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class DoelstellingAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDoelstellingAllPropertiesEquals(Doelstelling expected, Doelstelling actual) {
        assertDoelstellingAutoGeneratedPropertiesEquals(expected, actual);
        assertDoelstellingAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDoelstellingAllUpdatablePropertiesEquals(Doelstelling expected, Doelstelling actual) {
        assertDoelstellingUpdatableFieldsEquals(expected, actual);
        assertDoelstellingUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDoelstellingAutoGeneratedPropertiesEquals(Doelstelling expected, Doelstelling actual) {
        assertThat(expected)
            .as("Verify Doelstelling auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDoelstellingUpdatableFieldsEquals(Doelstelling expected, Doelstelling actual) {
        assertThat(expected)
            .as("Verify Doelstelling relevant properties")
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDoelstellingUpdatableRelationshipsEquals(Doelstelling expected, Doelstelling actual) {
        assertThat(expected)
            .as("Verify Doelstelling relationships")
            .satisfies(
                e ->
                    assertThat(e.getIsvansoortDoelstellingsoort())
                        .as("check isvansoortDoelstellingsoort")
                        .isEqualTo(actual.getIsvansoortDoelstellingsoort())
            )
            .satisfies(e -> assertThat(e.getHeeftHoofdstuk()).as("check heeftHoofdstuk").isEqualTo(actual.getHeeftHoofdstuk()));
    }
}
