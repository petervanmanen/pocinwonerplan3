package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class KoppelingAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoppelingAllPropertiesEquals(Koppeling expected, Koppeling actual) {
        assertKoppelingAutoGeneratedPropertiesEquals(expected, actual);
        assertKoppelingAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoppelingAllUpdatablePropertiesEquals(Koppeling expected, Koppeling actual) {
        assertKoppelingUpdatableFieldsEquals(expected, actual);
        assertKoppelingUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoppelingAutoGeneratedPropertiesEquals(Koppeling expected, Koppeling actual) {
        assertThat(expected)
            .as("Verify Koppeling auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoppelingUpdatableFieldsEquals(Koppeling expected, Koppeling actual) {
        assertThat(expected)
            .as("Verify Koppeling relevant properties")
            .satisfies(e -> assertThat(e.getBeschrijving()).as("check beschrijving").isEqualTo(actual.getBeschrijving()))
            .satisfies(e -> assertThat(e.getDirect()).as("check direct").isEqualTo(actual.getDirect()))
            .satisfies(e -> assertThat(e.getToelichting()).as("check toelichting").isEqualTo(actual.getToelichting()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoppelingUpdatableRelationshipsEquals(Koppeling expected, Koppeling actual) {}
}
