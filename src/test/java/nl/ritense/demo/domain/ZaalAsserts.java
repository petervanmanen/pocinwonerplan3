package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ZaalAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertZaalAllPropertiesEquals(Zaal expected, Zaal actual) {
        assertZaalAutoGeneratedPropertiesEquals(expected, actual);
        assertZaalAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertZaalAllUpdatablePropertiesEquals(Zaal expected, Zaal actual) {
        assertZaalUpdatableFieldsEquals(expected, actual);
        assertZaalUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertZaalAutoGeneratedPropertiesEquals(Zaal expected, Zaal actual) {
        assertThat(expected)
            .as("Verify Zaal auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertZaalUpdatableFieldsEquals(Zaal expected, Zaal actual) {
        assertThat(expected)
            .as("Verify Zaal relevant properties")
            .satisfies(e -> assertThat(e.getCapaciteit()).as("check capaciteit").isEqualTo(actual.getCapaciteit()))
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getNummer()).as("check nummer").isEqualTo(actual.getNummer()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertZaalUpdatableRelationshipsEquals(Zaal expected, Zaal actual) {}
}
