package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class LeefgebiedAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeefgebiedAllPropertiesEquals(Leefgebied expected, Leefgebied actual) {
        assertLeefgebiedAutoGeneratedPropertiesEquals(expected, actual);
        assertLeefgebiedAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeefgebiedAllUpdatablePropertiesEquals(Leefgebied expected, Leefgebied actual) {
        assertLeefgebiedUpdatableFieldsEquals(expected, actual);
        assertLeefgebiedUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeefgebiedAutoGeneratedPropertiesEquals(Leefgebied expected, Leefgebied actual) {
        assertThat(expected)
            .as("Verify Leefgebied auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeefgebiedUpdatableFieldsEquals(Leefgebied expected, Leefgebied actual) {
        assertThat(expected)
            .as("Verify Leefgebied relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLeefgebiedUpdatableRelationshipsEquals(Leefgebied expected, Leefgebied actual) {}
}
