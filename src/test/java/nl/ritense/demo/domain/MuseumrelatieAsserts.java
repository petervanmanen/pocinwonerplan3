package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class MuseumrelatieAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMuseumrelatieAllPropertiesEquals(Museumrelatie expected, Museumrelatie actual) {
        assertMuseumrelatieAutoGeneratedPropertiesEquals(expected, actual);
        assertMuseumrelatieAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMuseumrelatieAllUpdatablePropertiesEquals(Museumrelatie expected, Museumrelatie actual) {
        assertMuseumrelatieUpdatableFieldsEquals(expected, actual);
        assertMuseumrelatieUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMuseumrelatieAutoGeneratedPropertiesEquals(Museumrelatie expected, Museumrelatie actual) {
        assertThat(expected)
            .as("Verify Museumrelatie auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMuseumrelatieUpdatableFieldsEquals(Museumrelatie expected, Museumrelatie actual) {
        assertThat(expected)
            .as("Verify Museumrelatie relevant properties")
            .satisfies(e -> assertThat(e.getRelatiesoort()).as("check relatiesoort").isEqualTo(actual.getRelatiesoort()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMuseumrelatieUpdatableRelationshipsEquals(Museumrelatie expected, Museumrelatie actual) {
        assertThat(expected)
            .as("Verify Museumrelatie relationships")
            .satisfies(
                e -> assertThat(e.getValtbinnenDoelgroeps()).as("check valtbinnenDoelgroeps").isEqualTo(actual.getValtbinnenDoelgroeps())
            )
            .satisfies(
                e -> assertThat(e.getVersturenaanMailings()).as("check versturenaanMailings").isEqualTo(actual.getVersturenaanMailings())
            );
    }
}
