package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class LenerAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLenerAllPropertiesEquals(Lener expected, Lener actual) {
        assertLenerAutoGeneratedPropertiesEquals(expected, actual);
        assertLenerAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLenerAllUpdatablePropertiesEquals(Lener expected, Lener actual) {
        assertLenerUpdatableFieldsEquals(expected, actual);
        assertLenerUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLenerAutoGeneratedPropertiesEquals(Lener expected, Lener actual) {
        assertThat(expected)
            .as("Verify Lener auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLenerUpdatableFieldsEquals(Lener expected, Lener actual) {
        assertThat(expected)
            .as("Verify Lener relevant properties")
            .satisfies(e -> assertThat(e.getOpmerkingen()).as("check opmerkingen").isEqualTo(actual.getOpmerkingen()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertLenerUpdatableRelationshipsEquals(Lener expected, Lener actual) {
        assertThat(expected)
            .as("Verify Lener relationships")
            .satisfies(e -> assertThat(e.getIsBruikleens()).as("check isBruikleens").isEqualTo(actual.getIsBruikleens()));
    }
}
