package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class DeclaratiesoortAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDeclaratiesoortAllPropertiesEquals(Declaratiesoort expected, Declaratiesoort actual) {
        assertDeclaratiesoortAutoGeneratedPropertiesEquals(expected, actual);
        assertDeclaratiesoortAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDeclaratiesoortAllUpdatablePropertiesEquals(Declaratiesoort expected, Declaratiesoort actual) {
        assertDeclaratiesoortUpdatableFieldsEquals(expected, actual);
        assertDeclaratiesoortUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDeclaratiesoortAutoGeneratedPropertiesEquals(Declaratiesoort expected, Declaratiesoort actual) {
        assertThat(expected)
            .as("Verify Declaratiesoort auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDeclaratiesoortUpdatableFieldsEquals(Declaratiesoort expected, Declaratiesoort actual) {
        assertThat(expected)
            .as("Verify Declaratiesoort relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertDeclaratiesoortUpdatableRelationshipsEquals(Declaratiesoort expected, Declaratiesoort actual) {}
}
