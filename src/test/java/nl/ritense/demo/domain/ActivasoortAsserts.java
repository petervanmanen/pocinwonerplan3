package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ActivasoortAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActivasoortAllPropertiesEquals(Activasoort expected, Activasoort actual) {
        assertActivasoortAutoGeneratedPropertiesEquals(expected, actual);
        assertActivasoortAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActivasoortAllUpdatablePropertiesEquals(Activasoort expected, Activasoort actual) {
        assertActivasoortUpdatableFieldsEquals(expected, actual);
        assertActivasoortUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActivasoortAutoGeneratedPropertiesEquals(Activasoort expected, Activasoort actual) {
        assertThat(expected)
            .as("Verify Activasoort auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActivasoortUpdatableFieldsEquals(Activasoort expected, Activasoort actual) {
        assertThat(expected)
            .as("Verify Activasoort relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActivasoortUpdatableRelationshipsEquals(Activasoort expected, Activasoort actual) {}
}
