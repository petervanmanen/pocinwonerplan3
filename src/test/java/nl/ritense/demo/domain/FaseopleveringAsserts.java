package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class FaseopleveringAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFaseopleveringAllPropertiesEquals(Faseoplevering expected, Faseoplevering actual) {
        assertFaseopleveringAutoGeneratedPropertiesEquals(expected, actual);
        assertFaseopleveringAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFaseopleveringAllUpdatablePropertiesEquals(Faseoplevering expected, Faseoplevering actual) {
        assertFaseopleveringUpdatableFieldsEquals(expected, actual);
        assertFaseopleveringUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFaseopleveringAutoGeneratedPropertiesEquals(Faseoplevering expected, Faseoplevering actual) {
        assertThat(expected)
            .as("Verify Faseoplevering auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFaseopleveringUpdatableFieldsEquals(Faseoplevering expected, Faseoplevering actual) {}

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFaseopleveringUpdatableRelationshipsEquals(Faseoplevering expected, Faseoplevering actual) {}
}
