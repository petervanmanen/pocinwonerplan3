package nl.ritense.demo.domain;

import static nl.ritense.demo.domain.AssertUtils.bigDecimalCompareTo;
import static org.assertj.core.api.Assertions.assertThat;

public class KoopwoningenAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoopwoningenAllPropertiesEquals(Koopwoningen expected, Koopwoningen actual) {
        assertKoopwoningenAutoGeneratedPropertiesEquals(expected, actual);
        assertKoopwoningenAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoopwoningenAllUpdatablePropertiesEquals(Koopwoningen expected, Koopwoningen actual) {
        assertKoopwoningenUpdatableFieldsEquals(expected, actual);
        assertKoopwoningenUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoopwoningenAutoGeneratedPropertiesEquals(Koopwoningen expected, Koopwoningen actual) {
        assertThat(expected)
            .as("Verify Koopwoningen auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoopwoningenUpdatableFieldsEquals(Koopwoningen expected, Koopwoningen actual) {
        assertThat(expected)
            .as("Verify Koopwoningen relevant properties")
            .satisfies(
                e ->
                    assertThat(e.getKoopprijs()).as("check koopprijs").usingComparator(bigDecimalCompareTo).isEqualTo(actual.getKoopprijs())
            );
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKoopwoningenUpdatableRelationshipsEquals(Koopwoningen expected, Koopwoningen actual) {}
}
