package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class AreaalAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertAreaalAllPropertiesEquals(Areaal expected, Areaal actual) {
        assertAreaalAutoGeneratedPropertiesEquals(expected, actual);
        assertAreaalAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertAreaalAllUpdatablePropertiesEquals(Areaal expected, Areaal actual) {
        assertAreaalUpdatableFieldsEquals(expected, actual);
        assertAreaalUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertAreaalAutoGeneratedPropertiesEquals(Areaal expected, Areaal actual) {
        assertThat(expected)
            .as("Verify Areaal auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertAreaalUpdatableFieldsEquals(Areaal expected, Areaal actual) {
        assertThat(expected)
            .as("Verify Areaal relevant properties")
            .satisfies(e -> assertThat(e.getGeometrie()).as("check geometrie").isEqualTo(actual.getGeometrie()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertAreaalUpdatableRelationshipsEquals(Areaal expected, Areaal actual) {
        assertThat(expected)
            .as("Verify Areaal relationships")
            .satisfies(e -> assertThat(e.getLigtinBuurts()).as("check ligtinBuurts").isEqualTo(actual.getLigtinBuurts()))
            .satisfies(e -> assertThat(e.getValtbinnenWijks()).as("check valtbinnenWijks").isEqualTo(actual.getValtbinnenWijks()))
            .satisfies(e -> assertThat(e.getBinnenSchouwrondes()).as("check binnenSchouwrondes").isEqualTo(actual.getBinnenSchouwrondes()));
    }
}
