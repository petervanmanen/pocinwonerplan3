package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class KwaliteitscatalogusopenbareruimteAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKwaliteitscatalogusopenbareruimteAllPropertiesEquals(
        Kwaliteitscatalogusopenbareruimte expected,
        Kwaliteitscatalogusopenbareruimte actual
    ) {
        assertKwaliteitscatalogusopenbareruimteAutoGeneratedPropertiesEquals(expected, actual);
        assertKwaliteitscatalogusopenbareruimteAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKwaliteitscatalogusopenbareruimteAllUpdatablePropertiesEquals(
        Kwaliteitscatalogusopenbareruimte expected,
        Kwaliteitscatalogusopenbareruimte actual
    ) {
        assertKwaliteitscatalogusopenbareruimteUpdatableFieldsEquals(expected, actual);
        assertKwaliteitscatalogusopenbareruimteUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKwaliteitscatalogusopenbareruimteAutoGeneratedPropertiesEquals(
        Kwaliteitscatalogusopenbareruimte expected,
        Kwaliteitscatalogusopenbareruimte actual
    ) {
        assertThat(expected)
            .as("Verify Kwaliteitscatalogusopenbareruimte auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKwaliteitscatalogusopenbareruimteUpdatableFieldsEquals(
        Kwaliteitscatalogusopenbareruimte expected,
        Kwaliteitscatalogusopenbareruimte actual
    ) {}

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKwaliteitscatalogusopenbareruimteUpdatableRelationshipsEquals(
        Kwaliteitscatalogusopenbareruimte expected,
        Kwaliteitscatalogusopenbareruimte actual
    ) {}
}
