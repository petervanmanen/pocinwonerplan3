package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class BelproviderAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelproviderAllPropertiesEquals(Belprovider expected, Belprovider actual) {
        assertBelproviderAutoGeneratedPropertiesEquals(expected, actual);
        assertBelproviderAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelproviderAllUpdatablePropertiesEquals(Belprovider expected, Belprovider actual) {
        assertBelproviderUpdatableFieldsEquals(expected, actual);
        assertBelproviderUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelproviderAutoGeneratedPropertiesEquals(Belprovider expected, Belprovider actual) {
        assertThat(expected)
            .as("Verify Belprovider auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelproviderUpdatableFieldsEquals(Belprovider expected, Belprovider actual) {
        assertThat(expected)
            .as("Verify Belprovider relevant properties")
            .satisfies(e -> assertThat(e.getCode()).as("check code").isEqualTo(actual.getCode()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertBelproviderUpdatableRelationshipsEquals(Belprovider expected, Belprovider actual) {}
}
