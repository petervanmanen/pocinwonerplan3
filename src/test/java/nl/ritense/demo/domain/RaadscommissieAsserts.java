package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class RaadscommissieAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRaadscommissieAllPropertiesEquals(Raadscommissie expected, Raadscommissie actual) {
        assertRaadscommissieAutoGeneratedPropertiesEquals(expected, actual);
        assertRaadscommissieAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRaadscommissieAllUpdatablePropertiesEquals(Raadscommissie expected, Raadscommissie actual) {
        assertRaadscommissieUpdatableFieldsEquals(expected, actual);
        assertRaadscommissieUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRaadscommissieAutoGeneratedPropertiesEquals(Raadscommissie expected, Raadscommissie actual) {
        assertThat(expected)
            .as("Verify Raadscommissie auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRaadscommissieUpdatableFieldsEquals(Raadscommissie expected, Raadscommissie actual) {
        assertThat(expected)
            .as("Verify Raadscommissie relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRaadscommissieUpdatableRelationshipsEquals(Raadscommissie expected, Raadscommissie actual) {
        assertThat(expected)
            .as("Verify Raadscommissie relationships")
            .satisfies(e -> assertThat(e.getIslidvanRaadslids()).as("check islidvanRaadslids").isEqualTo(actual.getIslidvanRaadslids()));
    }
}
