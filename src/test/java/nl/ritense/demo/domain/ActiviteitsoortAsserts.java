package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ActiviteitsoortAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActiviteitsoortAllPropertiesEquals(Activiteitsoort expected, Activiteitsoort actual) {
        assertActiviteitsoortAutoGeneratedPropertiesEquals(expected, actual);
        assertActiviteitsoortAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActiviteitsoortAllUpdatablePropertiesEquals(Activiteitsoort expected, Activiteitsoort actual) {
        assertActiviteitsoortUpdatableFieldsEquals(expected, actual);
        assertActiviteitsoortUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActiviteitsoortAutoGeneratedPropertiesEquals(Activiteitsoort expected, Activiteitsoort actual) {
        assertThat(expected)
            .as("Verify Activiteitsoort auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActiviteitsoortUpdatableFieldsEquals(Activiteitsoort expected, Activiteitsoort actual) {
        assertThat(expected)
            .as("Verify Activiteitsoort relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertActiviteitsoortUpdatableRelationshipsEquals(Activiteitsoort expected, Activiteitsoort actual) {}
}
