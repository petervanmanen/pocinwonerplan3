package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class OmzetgroepAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmzetgroepAllPropertiesEquals(Omzetgroep expected, Omzetgroep actual) {
        assertOmzetgroepAutoGeneratedPropertiesEquals(expected, actual);
        assertOmzetgroepAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmzetgroepAllUpdatablePropertiesEquals(Omzetgroep expected, Omzetgroep actual) {
        assertOmzetgroepUpdatableFieldsEquals(expected, actual);
        assertOmzetgroepUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmzetgroepAutoGeneratedPropertiesEquals(Omzetgroep expected, Omzetgroep actual) {
        assertThat(expected)
            .as("Verify Omzetgroep auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmzetgroepUpdatableFieldsEquals(Omzetgroep expected, Omzetgroep actual) {
        assertThat(expected)
            .as("Verify Omzetgroep relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertOmzetgroepUpdatableRelationshipsEquals(Omzetgroep expected, Omzetgroep actual) {
        assertThat(expected)
            .as("Verify Omzetgroep relationships")
            .satisfies(e -> assertThat(e.getValtbinnenProducts()).as("check valtbinnenProducts").isEqualTo(actual.getValtbinnenProducts()));
    }
}
