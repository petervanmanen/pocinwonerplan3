package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class WozbelangAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWozbelangAllPropertiesEquals(Wozbelang expected, Wozbelang actual) {
        assertWozbelangAutoGeneratedPropertiesEquals(expected, actual);
        assertWozbelangAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWozbelangAllUpdatablePropertiesEquals(Wozbelang expected, Wozbelang actual) {
        assertWozbelangUpdatableFieldsEquals(expected, actual);
        assertWozbelangUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWozbelangAutoGeneratedPropertiesEquals(Wozbelang expected, Wozbelang actual) {
        assertThat(expected)
            .as("Verify Wozbelang auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWozbelangUpdatableFieldsEquals(Wozbelang expected, Wozbelang actual) {
        assertThat(expected)
            .as("Verify Wozbelang relevant properties")
            .satisfies(
                e -> assertThat(e.getDatumbegingeldigheid()).as("check datumbegingeldigheid").isEqualTo(actual.getDatumbegingeldigheid())
            )
            .satisfies(
                e -> assertThat(e.getDatumeindegeldigheid()).as("check datumeindegeldigheid").isEqualTo(actual.getDatumeindegeldigheid())
            )
            .satisfies(e -> assertThat(e.getEigenaargebruiker()).as("check eigenaargebruiker").isEqualTo(actual.getEigenaargebruiker()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertWozbelangUpdatableRelationshipsEquals(Wozbelang expected, Wozbelang actual) {}
}
