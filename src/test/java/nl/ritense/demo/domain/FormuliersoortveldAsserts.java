package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class FormuliersoortveldAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormuliersoortveldAllPropertiesEquals(Formuliersoortveld expected, Formuliersoortveld actual) {
        assertFormuliersoortveldAutoGeneratedPropertiesEquals(expected, actual);
        assertFormuliersoortveldAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormuliersoortveldAllUpdatablePropertiesEquals(Formuliersoortveld expected, Formuliersoortveld actual) {
        assertFormuliersoortveldUpdatableFieldsEquals(expected, actual);
        assertFormuliersoortveldUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormuliersoortveldAutoGeneratedPropertiesEquals(Formuliersoortveld expected, Formuliersoortveld actual) {
        assertThat(expected)
            .as("Verify Formuliersoortveld auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormuliersoortveldUpdatableFieldsEquals(Formuliersoortveld expected, Formuliersoortveld actual) {
        assertThat(expected)
            .as("Verify Formuliersoortveld relevant properties")
            .satisfies(e -> assertThat(e.getHelptekst()).as("check helptekst").isEqualTo(actual.getHelptekst()))
            .satisfies(e -> assertThat(e.getIsverplicht()).as("check isverplicht").isEqualTo(actual.getIsverplicht()))
            .satisfies(e -> assertThat(e.getLabel()).as("check label").isEqualTo(actual.getLabel()))
            .satisfies(e -> assertThat(e.getMaxlengte()).as("check maxlengte").isEqualTo(actual.getMaxlengte()))
            .satisfies(e -> assertThat(e.getVeldnaam()).as("check veldnaam").isEqualTo(actual.getVeldnaam()))
            .satisfies(e -> assertThat(e.getVeldtype()).as("check veldtype").isEqualTo(actual.getVeldtype()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertFormuliersoortveldUpdatableRelationshipsEquals(Formuliersoortveld expected, Formuliersoortveld actual) {
        assertThat(expected)
            .as("Verify Formuliersoortveld relationships")
            .satisfies(
                e ->
                    assertThat(e.getHeeftveldenFormuliersoort())
                        .as("check heeftveldenFormuliersoort")
                        .isEqualTo(actual.getHeeftveldenFormuliersoort())
            );
    }
}
