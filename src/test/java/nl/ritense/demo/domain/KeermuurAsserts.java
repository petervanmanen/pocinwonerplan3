package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class KeermuurAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeermuurAllPropertiesEquals(Keermuur expected, Keermuur actual) {
        assertKeermuurAutoGeneratedPropertiesEquals(expected, actual);
        assertKeermuurAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeermuurAllUpdatablePropertiesEquals(Keermuur expected, Keermuur actual) {
        assertKeermuurUpdatableFieldsEquals(expected, actual);
        assertKeermuurUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeermuurAutoGeneratedPropertiesEquals(Keermuur expected, Keermuur actual) {
        assertThat(expected)
            .as("Verify Keermuur auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeermuurUpdatableFieldsEquals(Keermuur expected, Keermuur actual) {
        assertThat(expected)
            .as("Verify Keermuur relevant properties")
            .satisfies(
                e -> assertThat(e.getBelastingklassenieuw()).as("check belastingklassenieuw").isEqualTo(actual.getBelastingklassenieuw())
            )
            .satisfies(e -> assertThat(e.getBelastingklasseoud()).as("check belastingklasseoud").isEqualTo(actual.getBelastingklasseoud()))
            .satisfies(e -> assertThat(e.getType()).as("check type").isEqualTo(actual.getType()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertKeermuurUpdatableRelationshipsEquals(Keermuur expected, Keermuur actual) {}
}
