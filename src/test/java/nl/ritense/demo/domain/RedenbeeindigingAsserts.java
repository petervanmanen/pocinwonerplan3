package nl.ritense.demo.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class RedenbeeindigingAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRedenbeeindigingAllPropertiesEquals(Redenbeeindiging expected, Redenbeeindiging actual) {
        assertRedenbeeindigingAutoGeneratedPropertiesEquals(expected, actual);
        assertRedenbeeindigingAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRedenbeeindigingAllUpdatablePropertiesEquals(Redenbeeindiging expected, Redenbeeindiging actual) {
        assertRedenbeeindigingUpdatableFieldsEquals(expected, actual);
        assertRedenbeeindigingUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRedenbeeindigingAutoGeneratedPropertiesEquals(Redenbeeindiging expected, Redenbeeindiging actual) {
        assertThat(expected)
            .as("Verify Redenbeeindiging auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRedenbeeindigingUpdatableFieldsEquals(Redenbeeindiging expected, Redenbeeindiging actual) {
        assertThat(expected)
            .as("Verify Redenbeeindiging relevant properties")
            .satisfies(e -> assertThat(e.getNaam()).as("check naam").isEqualTo(actual.getNaam()))
            .satisfies(e -> assertThat(e.getOmschrijving()).as("check omschrijving").isEqualTo(actual.getOmschrijving()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertRedenbeeindigingUpdatableRelationshipsEquals(Redenbeeindiging expected, Redenbeeindiging actual) {}
}
